#include <stdlib.h>
#include <stdio.h>

/**
 * main - The code
 *
 * Return: End all mains with 1
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
		printf("%d", a);

	putchar(10);

	return (1);
}
