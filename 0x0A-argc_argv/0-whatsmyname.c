#include <stdlib.h>
#include <stdio.h>

/**
 * main - The code
 * @argc: number of inputs
 * @argv: array of inputs
 * Return: End all mains with 0
 */
int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);

	return (argc);
}
